// A static logger class to log messages in the application.

class Logger {
  static void log(String message) {
    // Here you can implement your logging logic, e.g., print to console or write to a file
    // ignore: avoid_print
    print(message);
  }
}